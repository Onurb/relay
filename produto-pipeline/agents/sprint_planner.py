# TODO: implementar na semana 1 — SprintPlanner Agent
from crewai import Agent


class SprintPlannerAgent:
    """Agente responsável por decompor o product brief em tarefas de sprint,
    estimar esforço, definir prioridades e criar issues no Linear com os campos
    corretos (estimate, label, cycle). Garante que o backlog está sempre atualizado.
    """

    def build(self) -> Agent:
        """Constrói e devolve a instância CrewAI do agente."""
        pass
